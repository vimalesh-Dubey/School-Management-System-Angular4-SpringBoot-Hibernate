import { DynamicCompRendererDirective } from './dynamic-comp-renderer.directive';

describe('DynamicCompRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicCompRendererDirective();
    expect(directive).toBeTruthy();
  });
});
